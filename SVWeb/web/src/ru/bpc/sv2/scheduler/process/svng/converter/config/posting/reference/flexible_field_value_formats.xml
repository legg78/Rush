<?xml version="1.0" encoding="UTF-8"?>
<reference>
    <!-- 'key' is the name of field, 'value' is the template of incoming source format -->
    <!-- template is described as '<format>, <income_value_template>' -->
    <!-- Supported values for <format> property: -->
    <!-- 'decimal' - the values which should be converted into FM000000000000000000.0000 -->
    <!-- 'number' - the values which should be converted into integer number -->
    <!-- 'string' - the default type, may be skipped to write here -->
    <!-- 'date' - the values which should be converted into date in format yyyy-MM-dd'T'HH:mm:ss -->
</reference>